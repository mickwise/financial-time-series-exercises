# Core numeric stack
numpy>=1.26
pandas>=2.2
scipy>=1.14
matplotlib>=3.9

# Stats / econometrics
statsmodels>=0.14

# Jupyter runtime (optional but convenient for new users)
ipykernel>=6.29
jupyterlab>=4.2

# (add others only when truly needed)
joblib>=1.5.1
